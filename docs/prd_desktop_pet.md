# 桌宠 PRD（冻结）

> 2026-08-27 初冻今日安排。2026-08-27 晚修订：北极星加上可托管的长时间任务，第一例为 MAA 一键长草。
> 2026-08-30：对齐「先账号状态、再规划」。扫仓规格 `v26_maa_depot_inventory_plan.md`，工程 `v26_maa_depot_inventory_tech.md`，今日刷什么 `v25_arknights_farm_plan.md`。
> 2026-08-31：气泡勾选必须能改；「更新数据」不是第 9 个勾选；不另建工具总表。看板加隐蔽日志入口（有问题标红，对话可分析）。
> 2026-08-31 晚：计划任务「授权一次」挡不住每次拉 `Launcher.exe` / `Arknights.exe` 的 UAC。推荐认已有窗口、不再代点 exe；模拟器另开。**只记 PRD，代码暂不改。**
> 2026-08-31 夜：V26 验收空仓当成功。隐蔽日志扩读 MAA 出错段；扫仓失败可同趟再试一次「只更新数据」。模型不决定重试。

## 0. 拍板

| 项 | 结论 |
|----|------|
| 北极星 | **两件事**：① 飞书今日安排 ② 可持续的长时间任务（第一例：明日方舟日常）。「今天刷什么」是 ② 之上的一层，不是第三件雇工 |
| 更远的愿景 | 按**不同账号状态**定制养成 / 日常清理 / 抽卡。成立前提是每号一份能查的账，不是模型先「懂明日方舟」。**不当本阶段范围** |
| 本阶段主任务 | **账号状态**：拿到、存对、能按今天查找。游戏参照表按规划用到哪张再接哪张。强度 / 抽卡 / 全玩法理解往后排 |
| 下一刀 | **森空岛用户模型**。开游戏/UAC 策略已记，**暂不改代码** |
| 整体顺序 | V26 空仓不写（挡验收）→ 看板隐蔽日志（含 MAA 出错段）→ 扫仓同趟再试一次 → 方舟壳只读「清日常会更新仓库」→（另开刀，未排期）不再代点 PC exe → 森空岛用户模型 → V25。会前打断、勾飞书待办更后；**不改日程** |
| 用户 | 仅作者本机一号先跑通。多号 = 换 uid / 换仓文件。现在不做多用户产品 |
| 脑子 | Atlas 只下发 / 停止 / 汇报；作业由本机官方 MAA 执行；账本用确定性代码，禁止模型口算库存或掉率 |
| 工具怎么管 | 现有 `Toolkit.register` + 各 `*_tools.py` 的 spec + system 合同。规程放 `skills/*/SKILL.md`，看板「技能」页只读列出。**不另建工具总表 / 能力平台 / 插件市场**。每加一个能力：模块注册 + 合同补一句何时调用；分析类再补一份技能 |
| MAA 通道 | 远程控制。V26：写 `TaskQueue.IsEnable` + 一条 `LinkStart`。远控没有「更新数据」type，所以跟清日常走，**不做第 9 个勾选** |
| PC 开游戏 / UAC | **已证伪**：看板「授权一次」只让 `schtasks /Run` 不弹；`ShellExecute Launcher.exe` 和再拉 `Arknights.exe` 仍会 UAC。推荐（未开工）：有「明日方舟」窗口则直接干；启动器在、游戏窗不在则失败可见（请在启动器点开始）；两边都不在也不代开 exe。冷启动带 UAC 的 exe 只由人点。**安卓模拟器 + ADB 本阶段不做** |
| 触发形态 | 对话 / 看板 / 气泡方舟 / 右击共用勾选。清日常对话必须调工具，不许装完成。不问刷什么时不单独扫仓 |

## 0.5 数据三层（定制化先分清再做）

| 层 | 雇它干什么 | 本阶段 |
|----|------------|--------|
| **账号状态** | 这号有哪些材料、理智、干员、周玉 / 保全额度、代理卡、药 | **Must**。仓走 V26。干员 / 理智 / 额度走森空岛（接口已验，代码未写）。代理卡、药：扫不到，缺键失败，不当 0 |
| **游戏参照表** | 物品 id↔中文、关卡理智、企鹅掉落、商店单价 | 规划器算到哪一行，再接哪一张。V26 只用 MAA `item_index.json` 译名。禁止 LLM 填掉率 / 单价 |
| **判断** | 谁强、该抽谁、玩法攻略、自动改关卡 | **本阶段不做**。`plan.target` 仍要用户写「练谁」。抽卡规划另开刀 |

不把 OperBox 当仓库。不把 427 名干员全文塞进 LLM。不先做整站百科 / RAG。

## 1. 问题

当博士坐在桌前时，有两类雇工：

1. **今日安排**：用一两眼或一句问知道飞书上该盯哪件，好把注意力放回工作。
2. **长时间任务**：有些事要盯半小时（明日方舟日常）。应对桌宠说一句、点看板、或右击选项卡片，由 MAA **一键长草**。桌宠只负责入口和进度，不占死对话轮。PC 客户端因反作弊每次拉 exe 都要 UAC；计划任务挡不住。未改代码前仍走现有「代开启动器 / 计划任务开游戏」。记下的目标形态：游戏窗已在才托管，不代点 exe。

方舟侧已经能清日常。接着要问「今天刷什么」时，缺的是**这号仓库是真的**。现在的凑合：手填 json、截图猜图标、或每晚点一次 MAA 小工具。新产品必须比「再点一次识别」更少打扰：跟已经在跑的清日常走。

挂了或清日常失败时，现在的凑合是自己翻 `desk_companion.log`、MAA `gui.log` / `asst.log` 或终端。看板要有一个**不抢主路径**的入口：有问题标红，点开看原文，或跟凯尔希说「看看日志 / 仓库怎么挂的」——分析必须基于工具读到的出错段（桌宠日志 + MAA 任务出错段），不许编。扫仓空结果不当今天已同步。重试由清日常线程按规则再跑一次更新数据，不由模型看着日志决定。

更远的「按号定制养成 / 抽卡」现在不雇。没有账号状态，强度再熟也是编。

## 2. 成功标准

- 飞书已登录：不打开飞书客户端也能正确说出下一件该盯的事。失败可见，零次编造日程。
- **清日常**：8 项名称与 MAA 一键长草对齐；**气泡、看板、右击、对话改的是同一份勾选**；细参数仍在 MAA 齿轮。失败不假装清完。开游戏/UAC 的目标形态见 §0「PC 开游戏」，**尚未改实现**。
- **本阶段（账号状态）**：清日常结束后，今天的材料能按中文名查到；不是今天、没有文件、或 `data` 一个键都没有 → 失败原文，不出现「当 0」，不把空扫标成今天。代理卡不在结果里就不假装有。
- 对话下令后立刻结束，进度走方舟气泡。V26 起等到整段 `LinkStart` 汇报再读仓。
- **日志**：看板有隐蔽入口；今日桌宠日志有崩溃 / 看门狗 / 明确失败、或 MAA 今日有「任务出错」则标红；点开是重点 + 桌宠 / MAA 分栏，可过滤，不是一个文本框。对话「看看日志 / 仓库怎么挂的」必须先 `read_recent_errors` 再 `read_skill(maa-log-analysis)`，只喂最近出错段；没有出错就说没有。技能全文在看板「技能」页可见。
- **GitHub**：看板 GitHub 页按账号下列出全部未归档仓库，每库一张卡片（数字、阶段、根据 milestone issue 写的总结）。没有路线图 issue 就写无法判断，不编。看板「生成本周复盘」用本周一到此刻的 commit / 已合并 PR / 已关 issue 当产出，飞书未完成待办 + 未合并 PR + 带 milestone 的未关 issue 当还欠的；对话总结某仓库最近提交必须先 `github_recent` 再 `read_skill(github-repo-summary)`，只在气泡说，不写飞书。
- 护栏：不链 MaaCore；不注入游戏；不自写仓库 OCR；不把整份日志、整份 `asst.log`、失败截图喂进模型；**模型不准下发重试**。

## 3. 非目标（原因）

- 安装包 / 多用户产品 / 跨平台 / 语音：同前
- **把 MaaCore DLL 链进 desk-companion**
- **另起 Maa-HTTP :8848**
- **桌宠自己点屏幕、不经 MAA**；Hook / 注入 Unity
- 桌宠自写仓库 OCR；把 OperBox 当库存账本
- **本阶段不做判断层**：角色强度榜、抽卡规划、百科 RAG
- **本阶段不写 V25 规划器、不写森空岛同步**（顺序在 §5）
- 「更新数据」做成第 9 个可关掉的勾选（清日常强制带上；界面最多一行只读说明）
- **另建工具总表 / 能力注册平台 / 插件市场**（Atlas Toolkit 已经是注册表；合同漏了就补合同）
- 日志搜索引擎、实时 tail 大窗、气泡常驻红点、一出错就自动喊模型
- **整段再跑一遍清日常**当扫仓失败的恢复（会重复作战 / 购物；Daily 还可能跳过 OCR）
- **模型根据日志分析决定要不要重试、改间隔、改主题模板**
- 自动改 MAA 关卡；桌宠内 MAA 齿轮页；写回飞书日程
- 肉鸽、生息：卡片上有、默认不勾，不是必跑
- **本阶段不把日常迁到安卓模拟器 / ADB**（PC 官服未弃；换通道要另开产品）
- **代点 UAC / 关安全桌面 / 漏洞绕过提升**
- 开游戏/UAC 目标形态：**暂不改代码**，等单独说开工

## 4. 已交付（基线）

问今日安排、今日看板、`today.json`、模板 nudge、流式气泡、人设与最近 N 条、本周复盘写飞书文档（生成时读 `weekly-retro` 技能）、换肤与托盘。看板 / 气泡三壳：今日、方舟、对话。

MAA：8 项勾选（气泡已能改）+ 开 PC 客户端 + 计划任务拉起。V26 写队列 + `LinkStart` + 读仓**代码已写，待验收**。账本 `arknights_account.json` 能从已有 `DepotData.json` 写入。本机 MAA 6.16.8。

已知缺口：对话能开游戏 / 清日常 / 改勾选 / 看日志。空 `DepotData.json` 不再盖 `depot_sync`。同趟再试扫仓、看板右键、空勾选仍扫仓待验收。每次代拉 `Launcher.exe` / `Arknights.exe` 仍弹 UAC；策略已记，未改。

## 5. 整体计划

### 立刻（挡验收）

气泡「方舟」页勾选：**已验收通过。**

### 搁置：PC 开游戏不再代点 exe（未开工）

**问题：** 授权一次计划任务之后，再拉启动器 / 游戏 exe 仍要 UAC。`schtasks /Run` 管不到 `ShellExecute Launcher.exe`，也挡不住反作弊对 `Arknights.exe` 再要提升。

**推荐（未写代码）：**

- 已有「明日方舟」窗口 → 零次新 UAC，直接让 MAA 干活（现实现已接近）
- 启动器在、游戏窗不在 → 不拉 `Arknights.exe`，失败原文：在启动器里点开始游戏后再清日常
- 启动器也不在 → 不代开 `Launcher.exe`，失败原文：先自己打开启动器

**不做（本阶段）：** 改连安卓模拟器 + ADB；代点 UAC。

### 现在（V26，待验收）

清日常写队列（桌宠勾选 → `IsEnable`，强制 UserDataUpdate：仓库开、间隔 Daily）+ 一条 `LinkStart`。结束后读今天的 `DepotData.json`，译名写入 `inventory`。译名失败、或 `data` 为空对象 → 整份不写、`depot_sync` 不动。`UpdateOperBox` 保持 GUI 原值，结果不读。`LinkStart` SUCCESS 不等于扫仓成功。

「更新数据」没有单独入口。验收后可在方舟壳加一行只读：「清日常结束会更新仓库（每天最多一次）」。

8 项与队列 TaskType：

| 桌宠与 MAA 显示名 | 队列 TaskType | 远控子 type |
|----|----|----|
| 开始唤醒 | StartUp | `LinkStart-WakeUp` |
| 自动公招 | Recruit | `LinkStart-Recruiting` |
| 基建换班 | Infrast | `LinkStart-Base` |
| 理智作战 | Fight | `LinkStart-Combat` |
| 信用购物 | Mall | `LinkStart-Mall` |
| 领取奖励 | Award | `LinkStart-Mission` |
| 自动肉鸽 | Roguelike | `LinkStart-AutoRoguelike` |
| 生息演算 | Reclamation | `LinkStart-Reclamation` |

### 其后：看板隐蔽日志（含 MAA 排查）

**When** 桌宠或清日常 / 扫仓挂了，作者不想自己翻三份 log  
**I want** 看板上一个不抢「今日 / 明日方舟」的入口，有问题就标红；问凯尔希时她读到的是出错原文  
**so I can** 知道是桌宠崩了、还是 MAA 仓库识别没进仓，而不是猜

| 项 | 选择 |
|----|------|
| 位置 | 看板页脚小字「日志」，不是新的主 Tab。平时 muted |
| 标红 | **确定性规则**，不用模型着色。今日 `desk_companion.log` 出现 `CRASH` / `WATCHDOG` / 以失败收尾的清日常，**或** `{MAA}/debug/gui.log` 今日出现 `任务出错` → 入口变红 |
| 点开 | 重点区（最多 3 条）+ 桌宠栏 / MAA 栏分开展示原文与路径，可按来源、类型过滤。没有出错：写明没有。失败截图路径可写给人看，**不把 png 喂模型** |
| 对话 | 工具只读最近出错段（有上限）。桌宠 log + `gui.log` 的 `任务出错` 段；需要 Depot 细节时再截 `asst.log` 同一次任务链（仍有上限，禁止整文件）。合同：问日志必须先 `read_recent_errors` 再 `read_skill(maa-log-analysis)`。没有出错就说没有，不编原因。分析按钮由人点，不自动弹 |
| 技能 | `desk-companion/skills/*/SKILL.md`；看板「技能」页列出全部。不是插件市场，不走 Atlas RSI SkillLibrary |
| 不自动 | 出错不自动弹 Agent、不在气泡常驻红点、**不根据分析下发 MAA 任务** |

验收（在原四条上加 MAA）：

- Given 今日日志无 CRASH/WATCHDOG/清日常失败，且 MAA 无今日任务出错，When 打开看板，Then 页脚「日志」不红。
- Given 有一条今日 CRASH，When 打开看板，Then 「日志」标红；点开能在桌宠栏看到该段原文，不和 MAA 糊在同一框。
- Given 用户说「看看日志」且有出错段，When 对话，Then 先 `read_recent_errors` 再 `read_skill`，解释里出现日志里的原文关键词；不出现「大概是网络」而无工具。
- Given 打开看板「技能」，When 加载成功，Then 能看见 `maa-log-analysis` 的何时用和正文。
- Given 日志里没有出错，When 问看看日志，Then 回答没有问题记录，不编造故障。
- Given MAA `gui.log` 今日有 `任务出错: 仓库识别`，When 问仓库怎么挂的，Then 工具读到该句；解释对得上「没进仓库 / 空 data」，不编成网络或森空岛。

### 再其后：扫仓同趟再试一次（不跟日志页绑在一起）

**When** 这一趟长草已经跑完，但仓库是空结果  
**I want** 同一趟清日常里再扫一次仓，不要我整段再点一次、也不要模型决定试不试  
**so I can** 从「主题点空 / 没进仓库」这种偶发里恢复，失败仍看得见

| 项 | 选择 |
|----|------|
| 触发 | 清日常线程里 `ingest_depot` 因空 `data`（或同等扫仓失败）拒绝写入。不是日志页、不是对话、不是模型 |
| 动作 | 只再下发一次「更新数据」：其它任务 `IsEnable=false`，`UserDataUpdate` 开、`TriggerInterval=EveryTime`（Daily 刚失败过，不改间隔会跳过 OCR），一条 `LinkStart`，再读仓 |
| 次数 | **最多 1 次**。仍空或仍失败 → 气泡失败原文，`depot_sync` 仍不动 |
| 收尾 | 无论成败，把 `TriggerInterval` 写回 `Daily`，把 8 项 `IsEnable` 写回桌宠勾选。禁止留下 EveryTime |
| 不做 | 整段重跑唤醒/作战/公招/商店；第三次；改 MAA 模板；让 Agent 调远控 |

验收：

- Given 第一趟 `DepotData.json` 是今天但 `data` 为空，When 清日常收尾，Then 自动再跑一次仅更新数据；仍空则气泡说扫仓失败，账本日期不是这一趟。
- Given 再试成功扫到件，When 结束，Then inventory 有货，`TriggerInterval` 回到 Daily。
- Given 用户只是打开看板日志或问「看看日志」，When 对话结束，Then 没有新的 `LinkStart`。

### 更后

0. 方舟壳只读「清日常会更新仓库」（验收空仓门槛之后）
1. **森空岛用户模型**
2. **V25 今天刷什么**
3. 另开：练谁；抽卡
4. 更后：会前打断；勾飞书待办完成

### Won't（到 V25 落地前都不做）

改日程；链 DLL；抄作业；桌宠内齿轮页；强度榜；百科 RAG；多用户；Atlas 内核求解；工具总表 / 插件市场；本阶段模拟器日常。

## 6. 最危险假设

1. 气泡勾选修完后，只勾唤醒+基建+信用再清日常，队列里肉鸽为 false。验证：`gui.new.json`。
2. 写完配置再计划任务拉起，会跑「更新数据」。验证：桌宠清日常后 `DepotData.json` 的 `syncTime` 是否为今天且 `data` 非空（当日已手点过则 Daily 可能跳过 OCR，日期仍是今天、有货算过；空 `data` 不算过）。
3. 日志标红过宽会导致入口永远红。验证：只有今日 CRASH / WATCHDOG / 清日常失败 / MAA 任务出错才红，启动成功的普通 `log()` 和远控轮询不红。
4. Agent 分析日志会编造。验证：工具没读到出错段时不准编原因；有 MAA `仓库识别` 时不准说成网络。
5. 同趟再试会变成整段重跑或留下 EveryTime。验证：重试队列只有 UserDataUpdate；结束后间隔回到 Daily。
6. 把「懂游戏」当成下一步会膨胀成百科。验证：仓能查龙门币、代理卡缺键失败。
7. 计划任务能去掉每次 UAC：**已否**。验证：授权后仍会因代拉 exe 弹窗。下一刀若开工「不代点 exe」，验收是游戏已开时清日常零 UAC、未开时失败可见。

## 7. 接口依据（不写进产品话术）

- 远程控制：[协议](https://docs.maa.plus/zh-cn/protocol/remote-control-schema.html)
- 更新数据：[用户手册](https://docs.maa.plus/zh-cn/manual/introduction/user-data-update.html)；与小工具共用 `data/DepotData.json`
- 本机日志：`desk-companion/desk_companion.log`（`logutil.LOG_PATH`）；MAA `{安装目录}/debug/gui.log`、`asst.log`
- 森空岛：官方 `zonai.skland.com`（cred 不进 git / 对话）
- GitHub：https://github.com/MaaAssistantArknights/MaaAssistantArknights

## 8. 竞品对照

观赏/整蛊雇的是屏幕上有东西动。OpenPets 雇的是插件平台。通用桌面 Agent 雇的是一箱子工具。攻略站雇的是「游戏知识」。我们雇 Atlas 当入口：**飞书管日历待办，MAA 管长草，账本管这号有什么**。知识表按需接入，不先做百科。工具按模块注册，不先做市场。两件雇工都要能离开对话轮还活着。
