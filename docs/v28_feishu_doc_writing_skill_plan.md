# V28 实施计划：今日总结按飞书写作技能生成

> 2026-08-31。只改「生成总结 → 写入飞书文档」。不做 Callout/分栏，不抄完整 lark-doc 体裁路由器。

## 0. 问题

**When** 点看板「生成总结」再写入飞书
**I want** 一篇能 10 秒扫完的工作纪要（结论先行，多条日程/待办成表）
**so I can** 比现在三段流水更好用，且写法可在技能页看见、可改

## 1. 非目标

Callout / 分栏 / 画板 / emoji；会议纪要 / 周报 / PRD 全开；对话里另开一条写文档链路；同趟再试扫仓。

## 2. Must

1. `skills/feishu-doc-writing/SKILL.md` 为结构与红线的唯一来源；看板「技能」页能看见。
2. `generate_today_summary` 先 `read_skill("feishu-doc-writing")`，把正文塞进 prompt；缺文件失败可见，不静默用旧三段。
3. 写入通道仍是 Markdown `docs +create`。只用标题、段落、列表、引用、表格、加粗。

## 3. 结构（写进技能，不写进第二份 prompt 当第二套规矩）

1. 开头引用块：今天最该盯的一件事（事项 + 截止）
2. `## 日程与待办`：多于一条用表（事项 / 截止 / 状态），一条用一句话
3. `## 进展`：只写今日对话材料里出现过的，结论先行
4. `## 还该盯的`：最多 2～3 条；没有写「无」

没有材料的节写「无」或「材料不足」，不编。
